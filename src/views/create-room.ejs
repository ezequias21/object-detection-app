<%- include('header'); -%>
<%- include('navbar'); -%>
<div class="container">
    <div class="card__container">


        <% if(locals.userId) {%>
        <div class="card horizontal">
            <div class="card-stacked">
                <div class="card-content">
                      <h5>Crie uma nova sala.</h5>
                        <form action="/create-room" method="POST">
                            <div class="row">
                                <div class="input-field col s12">
                                    <input id="createRoomCode" name="createRoomCode" type="text" class="validate">
                                    <label for="createRoomCode">Código da sala</label>
                                </div>
                                <div class="input-field col btn-action__container">
                                    <button type="button" id="generateRoomCode" class="waves-effect waves-light blue btn">Gerar</button>
                                    <button type="submit" class="waves-effect waves-light blue btn">Criar</button>
                                </div>
                            </div>
                        </form>
                    </div>
                    <div class="card-action">
                        <i class="material-icons">info_outline</i>
                        <p>Ao criar uma nova sala, a última sala criada será removida.</p>
                      </div>
                  </div>
                  </div>
                  <% } %>



                  <div class="card horizontal">
                  <div class="card-stacked">
                  <div class="card-content">
                    <h5>Entre uma nova sala.</h5>
                      <form action="enter-room" method="POST">
                        <div class="row">
                            <div class="input-field col s12">
                                <input id="roomCode" name="roomCode" type="text" class="validate">
                                <label for="roomCode">Código da sala</label>
                            </div>
                            <div class="input-field col btn-action__container">
                                <button type="submit" class="waves-effect waves-light blue btn">Entrar</button>
                            </div>
                        </div>
                        </form>
                  </div>
                  <div class="card-action">
                    <i class="material-icons">info_outline</i>
                    <p>Só é possível entrar em salas que já existem. Crie uma nova sala, se necessário. </p>
                  </div>
                </div>
            </div>
      </div>
  </div>
  <script src="/enter-room.js" defer></script>
<%- include('footer'); -%>
